import { createBoard } from '@wixc3/react-board';
import { Menu } from 'semantic-ui-react';

// board cover
import cover from './menu.png';

export default createBoard({
    name: 'Menu',
    Board: () => (
        <Menu
            defaultActiveIndex={0}
            items={[
                <Menu.Item key="editorials" name="editorials">
                    Editorials
                </Menu.Item>,
                <Menu.Item key="reviews" name="reviews">
                    Reviews
                </Menu.Item>,
                <Menu.Item key="upcomingEvents" name="upcomingEvents">
                    Upcoming Events
                </Menu.Item>,
            ]}
        ></Menu>
    ),
    tags: ['Menu'],
    cover,
    environmentProps: {
        canvasPadding: { top: 10, bottom: 10, right: 10, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
