import { createBoard } from '@wixc3/react-board';
import { Search } from 'semantic-ui-react';

// board cover
import cover from './search.png';

export default createBoard({
    name: 'Search',
    Board: () => (
        <Search
            results={[
                { title: 'Result with no description' },
                { title: 'Result with description', description: 'A description of result' },
                { description: 'An independent, and a little rebellious description' },
            ]}
        />
    ),
    tags: ['Dropdown', 'Select', 'Search'],
    cover,
    environmentProps: {
        canvasPadding: { top: 10, bottom: 10, right: 10, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
