import { createBoard } from '@wixc3/react-board';
import { Rating } from 'semantic-ui-react';

// board cover
import cover from './rating.png';

export default createBoard({
    name: 'Rating',
    Board: () => <Rating />,
    tags: ['Rating'],
    cover,
    environmentProps: {
        canvasPadding: { top: 12, bottom: 10, right: 12, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
