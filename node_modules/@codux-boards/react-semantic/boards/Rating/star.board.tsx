import { createBoard } from '@wixc3/react-board';
import { Rating } from 'semantic-ui-react';

// board cover
import cover from './star.png';

export default createBoard({
    name: 'Star',
    Board: () => <Rating icon="star" defaultRating={3} maxRating={4} />,
    tags: ['Star', 'Rating'],
    cover,
    environmentProps: {
        canvasPadding: { top: 12, bottom: 10, right: 10, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
