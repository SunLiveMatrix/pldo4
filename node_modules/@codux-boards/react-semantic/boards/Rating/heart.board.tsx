import { createBoard } from '@wixc3/react-board';
import { Rating } from 'semantic-ui-react';

// board cover
import cover from './heart.png';

export default createBoard({
    name: 'Heart',
    Board: () => <Rating icon="heart" defaultRating={1} maxRating={3} />,
    tags: ['Heart', 'Rating'],
    cover,
    environmentProps: {
        canvasPadding: { top: 13, bottom: 10, right: 10, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
