import { createBoard } from '@wixc3/react-board';
import { Label } from 'semantic-ui-react';

// board cover
import cover from './circular.png';

export default createBoard({
    name: 'Circular',
    Board: () => <Label circular color="red" size="tiny" />,
    tags: ['circular', 'label'],
    cover,
    environmentProps: {
        canvasPadding: { top: 25, bottom: 20, right: 24, left: 20 },
        windowBackgroundColor: '#fcfcff',
    },
});
