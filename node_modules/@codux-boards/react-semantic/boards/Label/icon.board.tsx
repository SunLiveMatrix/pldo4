import { createBoard } from '@wixc3/react-board';
import { Icon, Label } from 'semantic-ui-react';

// board cover
import cover from './icon.png';

export default createBoard({
    name: 'Icon',
    Board: () => (
        <Label>
            <Icon name="mail" />
            Mail
        </Label>
    ),
    tags: ['Icon', 'Label'],
    cover,
    environmentProps: {
        canvasPadding: { top: 22, bottom: 20, right: 22, left: 20 },
        windowBackgroundColor: '#fcfcff',
    },
});
