import { createBoard } from '@wixc3/react-board';
import { Image } from 'semantic-ui-react';

// board cover
import cover from './corner.png';

export default createBoard({
    name: 'Corner',
    Board: () => (
        <Image
            fluid
            label={{ color: 'red', corner: 'left', icon: 'heart' }}
            src="https://react.semantic-ui.com/images/wireframe/image.png"
        />
    ),
    tags: ['image', 'corner'],
    cover,
    environmentProps: {
        windowBackgroundColor: '#fcfcff',
        canvasWidth: 500,
        canvasHeight: 420,
        canvasPadding: {
            top: 10,
            right: 12,
            bottom: 10,
            left: 10,
        },
    },
});
