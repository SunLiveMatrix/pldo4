import { createBoard } from '@wixc3/react-board';
import { Icon, Label, Menu } from 'semantic-ui-react';

// board cover
import cover from './floating.png';

export default createBoard({
    name: 'Floating',
    Board: () => (
        <Menu compact>
            <Menu.Item>
                <Icon name="mail" /> Messages
                <Label color="red" floating>
                    22
                </Label>
            </Menu.Item>
        </Menu>
    ),
    tags: ['Icon', 'Label', 'Menu', 'Floating'],
    cover,
    environmentProps: {
        canvasPadding: { top: 20, bottom: 20, right: 22, left: 15 },
        windowBackgroundColor: '#fcfcff',
    },
});
