import { createBoard } from '@wixc3/react-board';
import { Form, Label } from 'semantic-ui-react';

// board cover
import cover from './pointing-right.png';

export default createBoard({
    name: 'Pointing right',
    Board: () => (
        <Form>
            <Form.Field inline>
                <Label pointing="right">Your password must be 6 characters or more</Label>
                <input type="password" placeholder="Password" />
            </Form.Field>
        </Form>
    ),
    tags: ['Right', 'Form', 'Label', 'Pointing'],
    cover,
    environmentProps: {
        canvasPadding: { top: 10, bottom: 10, right: 14, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
