import { createBoard } from '@wixc3/react-board';
import { Input } from 'semantic-ui-react';

// board cover
import cover from './action-labeled-button.png';

export default createBoard({
    name: 'Action labeled button',
    Board: () => (
        <Input
            action={{
                color: 'teal',
                labelPosition: 'right',
                icon: 'copy',
                content: 'Copy',
            }}
            defaultValue="http://ww.short.url/c0opq"
        />
    ),

    tags: ['input', 'action', 'label'],
    cover,
    environmentProps: {
        canvasPadding: { top: 10, bottom: 10, right: 10, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
