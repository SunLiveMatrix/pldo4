import { createBoard } from '@wixc3/react-board';
import { Progress } from 'semantic-ui-react';

// board cover
import cover from './standard.png';

export default createBoard({
    name: 'Standard',
    Board: () => (
        <div style={{ width: ' 450px' }}>
            <Progress percent={20} />
        </div>
    ),
    tags: ['Progress'],
    cover,
    environmentProps: {
        canvasPadding: { top: 18, bottom: 0, right: 10, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
