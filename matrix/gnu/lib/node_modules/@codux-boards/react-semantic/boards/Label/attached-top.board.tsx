import { createBoard } from '@wixc3/react-board';
import { Image, Label, Segment } from 'semantic-ui-react';

// board cover
import cover from './attached-top.png';

export default createBoard({
    name: 'Attached Top',
    Board: () => (
        <Segment padded>
            <Label attached="top">HTML</Label>
            <Image src="https://react.semantic-ui.com/images/wireframe/paragraph.png" />
        </Segment>
    ),
    tags: ['Image', 'Label', 'Segment', 'top'],
    cover,
    environmentProps: {
        canvasPadding: { top: 10, bottom: 10, right: 10, left: 10 },
        windowBackgroundColor: '#fcfcff',
    },
});
