import { createBoard } from '@wixc3/react-board';
import { Button, Icon } from 'semantic-ui-react';

// board cover
import cover from './icon.png';

export default createBoard({
    name: 'Icon',
    Board: () => (
        <Button icon>
            <Icon name="world" />
        </Button>
    ),
    tags: ['Icon', 'Button'],
    cover,
    environmentProps: {
        canvasPadding: { top: 25, bottom: 25, right: 25, left: 25 },
        windowBackgroundColor: '#fcfcff',
    },
});
