import { createBoard } from '@wixc3/react-board';
import { Progress } from 'semantic-ui-react';

// board cover
import cover from './indicating.png';

export default createBoard({
    name: 'Indicating',
    Board: () => (
        <div style={{ width: ' 450px' }}>
            <Progress percent={20} indicating />
        </div>
    ),
    tags: ['Button', 'Progress'],
    cover,
    environmentProps: {
        canvasPadding: { top: 20, bottom: 0, right: 12, left: 12 },
        windowBackgroundColor: '#fcfcff',
    },
});
